/**
 * ⚠️ Archivo obsoleto — no añadir nada aquí.
 *
 * TypeScript excluye automáticamente los `.d.ts` que comparten nombre con un
 * `.ts` incluido (los interpreta como archivos de salida de `preload.ts`), así
 * que las declaraciones puestas aquí NUNCA llegaban a aplicarse: por eso
 * `window.wienerApi` aparecía sin tipar en toda la interfaz.
 *
 * El contrato está en  `src/shared/api.ts`
 * La declaración global está en  `src/shared/global.d.ts`
 */

export {};
